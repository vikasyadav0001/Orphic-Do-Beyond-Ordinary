# ================================================================
# Project Orphic — Requirements
# Install: pip install -r requirements.txt
# ================================================================

# ── LangGraph & LangChain (Core Agent Framework) ────────────────
langgraph
langchain
langchain-core
langchain-openai
langchain-google-genai
langchain-anthropic
langchain-community
langchain-groq


# ── PostgreSQL (Production Memory & DB) ─────────────────────────
psycopg[pool]       # async PostgreSQL driver
psycopg-pool               # async connection pool
langgraph-checkpoint-postgres  # LangGraph Postgres checkpointer
sqlalchemy[asyncio]        # ORM for user/session models
asyncpg                    # high-performance async Postgres driver
langchain-postgres
alembic

# ── Vector Memory (Long-term RAG Memory) ────────────────────────
pgvector                   # PostgreSQL vector extension python client
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers      # local embedding models
pymupdf
pymupdf4llm
langchain-pymupdf4llm


# ── API Framework ────────────────────────────────────────────────
fastapi[standard]
uvicorn[standard]          # ASGI server for FastAPI
websockets                 # WebSocket support
python-jose[cryptography]  # JWT auth tokens
passlib[bcrypt]            # password hashing
fastapi-users[sqlalchemy]
fastapi-users-db-sqlalchemy

# ─ Production WSGI/ASGI process manager ────────────────────────────────────────────────────────
gunicorn 

# ── Tools ────────────────────────────────────────────────────────
tavily-python              # Tavily web search
beautifulsoup4             # web scraping
requests
httpx                      # async HTTP client
aiosqlite
pandas

# ── MCP Protocol ─────────────────────────────────────────────────
mcp                        # Model Context Protocol SDK
fastmcp
langchain_mcp_adapters

# ── Observability & Logging ──────────────────────────────────────
langsmith                  # LangChain tracing & observability
python-dotenv              # .env file loading
structlog                  # structured logging

# ── Utilities ────────────────────────────────────────────────────
pydantic                   # data validation & settings
pydantic-settings
tiktoken                   # OpenAI token counting
